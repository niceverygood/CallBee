{
  "name": "@colli/db",
  "version": "0.1.0",
  "private": true,
  "description": "Colli-BoBi Prisma 스키마 + 클라이언트 (신규 테이블). 구독자/구독은 어댑터 경유 읽기전용.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "generate": "prisma generate",
    "migrate": "prisma migrate dev",
    "migrate:deploy": "prisma migrate deploy",
    "studio": "prisma studio",
    "typecheck": "tsc --noEmit",
    "test": "echo \"(db) no runtime tests\" && exit 0"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "prisma": "^5.22.0",
    "typescript": "^5.6.3"
  }
}
