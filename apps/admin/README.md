# @colli/admin — Worker E — Admin Dashboard

스캐폴딩 스텁. 착수 전 반드시 읽을 것: 루트 `CLAUDE.md`, `docs/worker-contracts.md`, `@colli/contracts`.

이 디렉토리는 해당 Worker 가 소유한다. 공유 타입/스키마는 `@colli/contracts` 에서만 import.
